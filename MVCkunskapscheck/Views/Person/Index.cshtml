@model IEnumerable<MVCkunskapscheck.Models.Person>

<p>
    <br />
    @Html.ActionLink("Create New", "Create")
</p>
<br />
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.namn)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.namn)

            
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.namn }) |
            @Html.ActionLink("Details", "Details", new { id = item.namn }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.namn }) |
            @Html.ActionLink("Fast Delete", "DeleteDirekt", new { id = item.namn })
        </td>

        <td>
            <table class="table">
                <tr>
                    <th>
                        Adresser
                    </th>
                    <th></th>
                </tr>

                @foreach (var it in item.Adresser)
                    {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => it.adress)


                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = it.adress }) |
                            @Html.ActionLink("Details", "Details", new { id = it.adress }) |
                            @Html.ActionLink("Delete", "Delete", new { id = it.adress })

                        </td>



                    </tr>

                    }

                
            </table>


        </td>



    </tr>
}

</table>
